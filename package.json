{
  "name": "local-meetup",
  "version": "0.1.0",
  "private": true,
  "description": "Local Meetup mobile prototype wrapped with Capacitor for iOS builds.",
  "scripts": {
    "build": "node scripts/build-web.js",
    "check": "node scripts/check.js",
    "cap:add:ios": "npx cap add ios",
    "cap:sync:ios": "npm run build && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
